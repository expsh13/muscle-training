{
	"name": "api",
	"version": "0.0.0",
	"description": "バックエンド（Hono）",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"build": "tsc && wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"type-check": "tsc --noEmit",
		"test": "vitest",
		"test:watch": "vitest --watch"
	},
	"dependencies": {
		"hono": "^3.12.0",
		"@muscle-training/types": "workspace:*",
		"@muscle-training/db": "workspace:*",
		"@muscle-training/utils": "workspace:*"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20231218.0",
		"wrangler": "^3.22.0",
		"typescript": "^5.3.0",
		"vitest": "^1.2.0"
	}
}
